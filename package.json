{
  "name": "avalon-online",
  "private": true,
  "version": "2.0.0",
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "dev": "npm run dev:server & npm run dev:client",
    "dev:server": "npm -w packages/server run dev",
    "dev:client": "npm -w packages/client run dev",
    "build": "npm -w packages/shared run build && npm -w packages/client run build && npm -w packages/server run build",
    "start": "npm -w packages/server run start",
    "typecheck": "npm -w packages/shared run typecheck && npm -w packages/server run typecheck && npm -w packages/client run typecheck"
  }
}
